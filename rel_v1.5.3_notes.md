# Booth Keeper v1.5.3 — 版本隔离（R20）：新版替换时旧版进 v{N}/ 子目录

## 新增：版本隔离
- 下载列表含**新版本**文件时（如 MoonlightPostKipV2.zip），自动把目录根的旧版本
  （V1 / Ver1.x 等）移入 `v{旧版本号}/` 子目录保留，新版本放根目录。
- 示例（8761569 ムーンライトポスト，作者已把 V1 更新为 V2）：
  ```
  8761569_…/
  ├── MoonlightPostKipV2.zip      ← 新版在根
  ├── …（其余 V2）
  └── v1/                         ← 旧版整套隔离保留
      ├── MoonlightPostKipV1.zip
      └── …（其余 V1）
  ```
- 已在批量链接页 + 巡检补全两处接入：下载前先比对新旧，旧版自动归档，
  不会出现「新版还没下、旧版残件占着坑」的混沌。

## 本次实际处置
- 8761569：V1 三件（含两个损坏残件）已全部移入 `v1/` 保留，
  V2 三件（71.8MB / 35.8MB / 35.9MB）已下载到目录根，完整性校验全部通过。

## 库健康度
- 全库损坏包仅剩 2 个无法自动修复：
  - 8701159：¥500 付费商品（不可免费修）
  - 8760977：下载解析 0 项（疑似受限/需登录）

## 两种下载
- **Windows 安装包** `BoothKeeper_Setup_1.5.3.exe`（77 MB）
- **zip 解压即用** `BoothKeeper_portable_v1.5.3.zip`（76 MB）